{
  "name": "market-echo",
  "version": "0.2.0",
  "private": true,
  "description": "Local-first historical price-pattern retrieval. Inspect cases, not predictions.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "example": "node scripts/example.mjs"
  }
}
